SLIDE_COUNT_PROMPT = """任务：决定最终采用的 PPT 页数。\n输出字段：mode, recommended_slide_count, accepted_slide_count, count_includes_cover, count_includes_agenda, count_includes_closing, reason, coverage_summary, confidence。\n只根据输入 JSON 的结构化约束决策，不重新理解原始材料。\n优先级：fixed 用户要求 > range 用户要求 > source fixed > source range > 自由推荐。\n规则：accepted_slide_count 必须严格服从最高优先级约束；recommended_slide_count 表示内容上合适的页数；reason 用一句话说明采用了哪级约束；coverage_summary 用一句话概括覆盖面。"""
